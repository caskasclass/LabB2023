package util;

import javafx.scene.image.Image;
import javafx.scene.image.PixelReader;
import javafx.scene.paint.Color;

public class ColorMixer {
    
    // a partire da una oggetto image restituisci il mix dei colori di quella immagine e resituisci un colore
    public static Color  getMixedColor(Image image) {
        // calcola il mix dei colori dell'immagine
       // PixelReader reader =  image.getPixelReader();
        // calcola il colore medio
        return null ;
        
    }
}
